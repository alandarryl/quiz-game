// app/quiz.tsx
import { View, Text } from 'react-native';

export default function QuizScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Écran de Quiz</Text>
    </View>
  );
}